{
  "version": 2,
  "framework": "nextjs",
  "buildCommand": "cd project && npm install && next build",
  "installCommand": "cd project && npm install",
  "outputDirectory": "project/.next",
  "rootDirectory": "project"
}
